// Gaya khas halaman Kamus. CSS Carbon bersamanya sudah dimuat di app/(shell)/layout.tsx.
import './kamus.scss';

export default function KamusLayout({ children }: { children: React.ReactNode }) {
  return <>{children}</>;
}
