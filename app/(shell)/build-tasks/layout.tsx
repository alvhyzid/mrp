// Gaya khas Daftar Tugas Pembangunan. CSS Carbon bersamanya dimuat di app/(shell)/layout.tsx.
import './build-tasks.scss';

export default function BuildTasksLayout({ children }: { children: React.ReactNode }) {
  return <>{children}</>;
}
