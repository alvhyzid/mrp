// Gaya khas halaman Apa yang Baru. CSS Carbon bersamanya sudah dimuat di app/(shell)/layout.tsx.
import './whats-new.scss';

export default function WhatsNewLayout({ children }: { children: React.ReactNode }) {
  return <>{children}</>;
}
