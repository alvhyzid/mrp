// Gaya khas Dashboard HRD. CSS Carbon bersamanya sudah dimuat di app/(shell)/layout.tsx.
import './hr.scss';

export default function HrLayout({ children }: { children: React.ReactNode }) {
  return <>{children}</>;
}
