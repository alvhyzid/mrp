// Gaya khas halaman Absensi. CSS Carbon bersamanya sudah dimuat di app/(shell)/layout.tsx.
import './attendance.scss';

export default function AttendanceLayout({ children }: { children: React.ReactNode }) {
  return <>{children}</>;
}
