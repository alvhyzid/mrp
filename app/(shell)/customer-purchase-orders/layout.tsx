// Gaya khas halaman PO klien. CSS Carbon bersamanya sudah dimuat di app/(shell)/layout.tsx.
import './customer-po.scss';

export default function CustomerPoLayout({ children }: { children: React.ReactNode }) {
  return <>{children}</>;
}
