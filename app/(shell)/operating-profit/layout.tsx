// Gaya khas halaman Laba Operasional. CSS Carbon bersamanya sudah dimuat di app/(shell)/layout.tsx.
import './operating-profit.scss';

export default function OperatingProfitLayout({ children }: { children: React.ReactNode }) {
  return <>{children}</>;
}
