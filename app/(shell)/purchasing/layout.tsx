// Gaya khas halaman Pembelian. CSS Carbon bersamanya dimuat di app/(shell)/layout.tsx.
import './purchasing.scss';

export default function PurchasingLayout({ children }: { children: React.ReactNode }) {
  return <>{children}</>;
}
