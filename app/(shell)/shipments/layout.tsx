// Gaya khas halaman Pengiriman. CSS Carbon bersamanya dimuat di app/(shell)/layout.tsx.
import './shipments.scss';

export default function ShipmentsLayout({ children }: { children: React.ReactNode }) {
  return <>{children}</>;
}
