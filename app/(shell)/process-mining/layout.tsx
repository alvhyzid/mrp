// Gaya khas halaman Process Mining. CSS Carbon bersamanya sudah dimuat di app/(shell)/layout.tsx.
import './process-mining.scss';

export default function ProcessMiningLayout({ children }: { children: React.ReactNode }) {
  return <>{children}</>;
}
