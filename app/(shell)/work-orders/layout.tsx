// Gaya khas halaman Work Order. CSS Carbon bersamanya dimuat di app/(shell)/layout.tsx.
import './work-orders.scss';

export default function WorkOrdersLayout({ children }: { children: React.ReactNode }) {
  return <>{children}</>;
}
