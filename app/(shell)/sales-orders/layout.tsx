// Gaya khas halaman Sales Order. CSS Carbon bersamanya dimuat di app/(shell)/layout.tsx.
import './sales-orders.scss';

export default function SalesOrdersLayout({ children }: { children: React.ReactNode }) {
  return <>{children}</>;
}
