// Gaya khas Dashboard Gudang. CSS Carbon bersamanya sudah dimuat di app/(shell)/layout.tsx.
import './warehouse.scss';

export default function WarehouseLayout({ children }: { children: React.ReactNode }) {
  return <>{children}</>;
}
