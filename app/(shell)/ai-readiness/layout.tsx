// Gaya khas halaman Kesiapan AI. CSS Carbon bersamanya sudah dimuat di app/(shell)/layout.tsx.
import './ai-readiness.scss';

export default function AiReadinessLayout({ children }: { children: React.ReactNode }) {
  return <>{children}</>;
}
