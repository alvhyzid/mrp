// Gaya khas Dashboard Proyek AI. CSS Carbon bersamanya sudah dimuat di app/(shell)/layout.tsx.
import './ai-project.scss';

export default function AiProjectLayout({ children }: { children: React.ReactNode }) {
  return <>{children}</>;
}
